name: Build bot docker

on:
  push:
    branches: [main]
    paths: [src/bot/**, .github/workflows/bot-build-docker.yml]
  pull_request:
    paths: [src/bot/**, .github/workflows/bot-build-docker.yml]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: src/bot/Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
