name: news-aggregator

services:
  backend:
    image: ghcr.io/neclor/news-aggregator/backend:latest
    container_name: news-aggregator-backend
    env_file: .env
    environment:
      DB_PATH: /data/news.db
      TG_BACKEND_SESSION_PATH: /data/backend_session
    ports:
      - "${SERVER_PORT:-8080}:8000"
    volumes:
      - ${HOST_DATA_DIR:-./data}:/data
    restart: unless-stopped
    networks:
      - news-aggregator-network
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          memory: 64M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  bot:
    image: ghcr.io/neclor/news-aggregator/bot:latest
    container_name: news-aggregator-bot
    env_file: .env
    environment:
      BACKEND_URL: http://backend:8000
      TG_BOT_SESSION_PATH: /data/bot_session
      BOT_DATA_PATH: /data/bot_data.json
    volumes:
      - ${HOST_DATA_DIR:-./data}:/data
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - news-aggregator-network
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 128M
        reservations:
          memory: 32M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  news-aggregator-network:
    driver: bridge
